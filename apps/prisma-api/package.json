{
  "name": "prisma-api",
  "version": "1.0.0",
  "scripts": {
    "prisma:migrate": "dotenv -e ../../.env -- prisma migrate dev --name init",
    "prisma:studio": "dotenv -e ../../.env -- prisma studio"
  },
  "dependencies": {
    "@prisma/client": "^6.5.0",
    "prisma": "^6.5.0"
  },
  "devDependencies": {
    "dotenv-cli": "^8.0.0"
  }
}
